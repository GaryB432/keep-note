import { describe, expect, it } from "vitest";

describe("Index stub", () => {
  it("should greet the whole world as one for now, whatever 'index' may be", () => {
    expect(2 + 4).toEqual(6);
  });
});
